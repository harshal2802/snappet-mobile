<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- User-initiated catalog download from a user-controlled static host (Kilter opt-in catalog,
         issue #42 Phase 2). The only network egress; no Snappet backend, nothing uploaded. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Bluetooth LE for connecting to a Kilter Board to light up climbs (Kilter mini-app, Phase 2)
         and to heart-rate bands. BLE is optional, so the app runs fine without it. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <!-- Kilter P1 board auto-detect: a coarse, when-in-use location fix powers the pre-connect "you're at
         your usual gym" arrival suggestion. COARSE only (≈110 m), bucketed on-device, never networked and
         never reverse-geocoded. Optional — denying it degrades gracefully to BLE-only recognition. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Camera is used to scan a receipt for the Split Expenses mini-app (system camera app, no
         permission), AND in-app via CameraX to scan a shared Kilter climb's QR (issue #91), which
         DOES require the CAMERA permission (requested at the point of use). Optional feature. -->
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Pomodoro (issue #85): the running phase shows an ongoing countdown notification from a
         foreground service (the Android counterpart of the iOS Live Activity), and an exact
         wake-from-Doze alarm posts the phase-end alert even if the process has died. All local;
         nothing leaves the device. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".SnappetApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Snappet"
        android:supportsRtl="true"
        android:theme="@style/Theme.Snappet">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Snappet"
            android:theme="@style/Theme.Snappet.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Issue #91: open a shared Kilter climb via snappet://kilter/climb/<uuid>?angle=<n>
                 (the same scheme iOS shares), routed by MainActivity into the Kilter module.
                 Issue #99: snappet://module/<id> opens a module/editor for static launcher shortcuts. -->
            <intent-filter android:label="Open in Snappet">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="snappet" android:host="kilter" />
                <data android:host="module" />
            </intent-filter>

            <!-- Static launcher shortcuts (issue #99). -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Glance launcher widgets (issue #99): habit (headless check-off) + focus (start). -->
        <receiver
            android:name=".widget.HabitWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/habit_widget_info" />
        </receiver>
        <receiver
            android:name=".widget.FocusWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/focus_widget_info" />
        </receiver>

        <!-- Pomodoro countdown (issue #85): foreground service for the ongoing chronometer
             notification, and the phase-boundary alarm receiver. -->
        <service
            android:name=".feature.pomodoro.PomodoroService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-started focus-timer countdown; shows the remaining time as an ongoing notification until the phase ends or the user pauses." />
        </service>
        <receiver
            android:name=".feature.pomodoro.PomodoroAlarmReceiver"
            android:exported="false" />
        <receiver
            android:name=".feature.pomodoro.PomodoroBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Shares the temporary receipt photo with the system camera app for OCR capture. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
